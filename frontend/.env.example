# GhostShift Frontend — local dev defaults
# Copy this file to `.env` and fill in your values.

# Backend API base URL.
# Default points at the FastAPI dev server on localhost:8000.
# In production, set this to your deployed backend (e.g. https://ghostshift-api.onrender.com/api).
VITE_API_URL=http://localhost:8000/api